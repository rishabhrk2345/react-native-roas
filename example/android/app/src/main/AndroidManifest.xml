<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Custom-scheme deep link, so the sample can exercise
             Roas.handleDeepLink() with a one-line adb command and no hosted
             assetlinks.json:
               adb shell am start -a android.intent.action.VIEW \
                 -d "roasrn://open?rsclid=test123"
             Mirrors the Flutter example's `roassample://open` filter. A real
             deployment should ALSO add an https:// App Links intent-filter
             (android:autoVerify="true") once the app has a domain with a
             hosted assetlinks.json — the custom scheme alone is a dev/QA
             convenience, not a substitute for it.

             launchMode is already singleTask above, which is what makes a
             warm-start link arrive through onNewIntent rather than starting a
             second activity; RN's Linking listener reads it from there. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="roasrn" android:host="open" />
        </intent-filter>
      </activity>
    </application>
</manifest>
